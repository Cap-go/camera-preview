{
  "name": "example",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview"
  },
  "dependencies": {
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/toast": "^6.0.0",
    "@capgo/camera-preview": "file:.."
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^4.0.0"
  }
}
